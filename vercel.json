{
  "buildCommand": "cd web && npm install --legacy-peer-deps && npx vite build",
  "outputDirectory": "web/dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
